<picture>
    {% for mq, source in mq_mappings | reverse %}
        <source srcset="{{ source }}" media="({{ mq }})">
    {% endfor %}
    <img src="{{ image.style }}" height="{{ image.height }}" width="{{ image.width }}" title="{{ image.title }}"
         alt="{{ image.alt }}"/>
</picture>