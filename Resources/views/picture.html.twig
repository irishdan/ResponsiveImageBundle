<picture>
    {% for set in image.picture %}
        <source srcset="{{ set.src }}" media="({{ set.mediaQuery }})">
    {% endfor %}
    <img src="{{ image.src }}" height="{{ image.height }}" width="{{ image.width }}" title="{{ image.title }}" alt="{{ image.alt }}"/>
</picture>